# 构建资源定位器

Creator 3.8.x 构建资源定位扩展。

## 使用帮助

输入构建文件路径、文件名或 Cocos UUID，插件会查找当前源资源、Meta 和 Git 历史中的旧资源，并区分构建文件与源文件。定位只读取 `assets`、`build` 和 Git 历史，不修改源文件，不访问网络或登录数据。

Creator 菜单：`扩展 -> 构建资源定位器 -> 打开使用帮助`。
