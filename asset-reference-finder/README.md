# 资源引用查找器

Creator 3.8.x 资源引用排查扩展。

## 使用帮助

在资源管理器选中 `assets` 下资源，使用 F6 或扩展菜单“查找资源引用”。面板显示引用树、Prefab/Scene 字符串匹配和扫描数量。Prefab 冗余清理只处理明确选中的 Prefab，并先创建备份；确认 Git 状态和备份后再执行。插件只读取 Asset DB 和项目文件，不访问网络、不读取用户隐私。

Creator 菜单：`扩展 -> 资源引用查找器 -> 打开使用帮助`。
